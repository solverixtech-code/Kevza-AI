const colors={green:'#08a979',blue:'#2466ff',purple:'#8b3cff',orange:'#ff8a00',red:'#ff3f62',pink:'#ec4899'};
const spark={green:[16,18,17,20,22,19,23,25,21,24,18,16,20,23,21,19,18],purple:[12,18,14,20,17,19,24,18,16,20,22,17,19,24,21,18,20],blue:[20,23,18,21,25,22,18,20,24,19,17,21,23,20,18,22,19],red:[22,18,20,17,15,18,21,16,19,22,18,16,20,23,19,17,21],orange:[15,18,24,20,17,21,26,19,18,22,20,17,24,27,21,19,23],pink:[14,18,17,21,19,16,20,23,18,15,19,21,18,17,16,14,15]};
function pts(data,w,h,p=2){const max=Math.max(...data),min=Math.min(...data),range=max-min||1;return data.map((v,i)=>`${p+i*(w-p*2)/(data.length-1)},${h-p-((v-min)/range)*(h-p*2)}`).join(' ')}
document.querySelectorAll('[data-spark]').forEach(svg=>{const key=svg.dataset.spark;svg.setAttribute('viewBox','0 0 150 28');svg.style.setProperty('--c',colors[key]);svg.innerHTML=`<polyline class="spark-line" points="${pts(spark[key],150,28)}"></polyline>`});
const score=document.querySelector('[data-chart="score"]');
if(score){const data=[82,88,92,89,86,93,91,90,94];const w=520,h=150,p={l:34,r:12,t:12,b:25};const sx=i=>p.l+i*(w-p.l-p.r)/(data.length-1);const sy=v=>h-p.b-((v-50)/50)*(h-p.t-p.b);let grid='';[100,90,80,70,60,50].forEach(v=>{const y=sy(v);grid+=`<line class="gridline" x1="${p.l}" y1="${y}" x2="${w-p.r}" y2="${y}"></line><text x="4" y="${y+3}">${v}%</text>`});const line=data.map((v,i)=>`${sx(i)},${sy(v)}`).join(' ');const area=`${p.l},${h-p.b} ${line} ${w-p.r},${h-p.b}`;const labels=['May 27','May 28','May 29','May 30','May 31','Jun 01','Jun 02'].map((d,i)=>`<text x="${sx(i+1)}" y="${h-6}" text-anchor="middle">${d}</text>`).join('');score.innerHTML=`<svg class="chart" viewBox="0 0 ${w} ${h}">${grid}<polygon class="score-fill" points="${area}"></polygon><polyline class="score-line" points="${line}"></polyline>${labels}</svg>`}
const dialog=document.getElementById('compliance-dialog');
document.querySelectorAll('[data-open]').forEach(btn=>btn.addEventListener('click',()=>{if(!dialog)return;dialog.querySelector('h2').textContent=btn.dataset.open.replace(/\b\w/g,c=>c.toUpperCase());dialog.querySelector('.dialog-body').innerHTML='<p>This compliance detail panel is ready for connecting to live compliance data.</p>';dialog.showModal();}));
document.querySelector('.menu-toggle')?.addEventListener('click',()=>document.querySelector('.compliance-shell')?.classList.toggle('menu-open'));
