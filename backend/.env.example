PORT=3000
FRONTEND_URL=http://localhost:5173
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/kevza_ai?schema=public
JWT_SECRET=replace-with-a-long-random-secret-before-production

# Optional SMTP email settings for OTP delivery.
# For Gmail, use an App Password, not your normal Gmail password.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM="KevzaAI <no-reply@kevza.ai>"
